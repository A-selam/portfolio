<footer class={`w-full flex justify-center py-10`}>
  <div
    class="layout-content-container flex flex-col w-full max-w-[800px] items-center px-6 md:px-0"
  >
    <div class="w-full h-[1px] bg-accent-line mb-8 opacity-30"></div>
    <div class="flex flex-col items-center">
      <p
        class="text-primary/50 text-[10px] font-mono tracking-widest text-center"
      >
        <span class="rolling-item block leading-none">2026</span>
        <span class="rolling-item block text-primary/20 leading-none">|</span>
        <span class="rolling-words" aria-hidden="true">
          <span class="rolling-track">
            <span class="rolling-item">Abduselam Sultan Zakir</span>
            <span class="rolling-item">@a-selam</span>
            <span class="rolling-item">Selam Sultan</span>
          </span>
        </span>
        <span class="sr-only">Abduselam Sultan Zakir</span>
      </p>
    </div>
  </div>
</footer>

<style>
  .rolling-words {
    display: inline-flex;
    height: 1.1rem;
    overflow: hidden;
  }

  .rolling-track {
    display: inline-flex;
    flex-direction: column;
    animation: rollNames 10s cubic-bezier(0.76, 0, 0.24, 1) infinite;
  }

  .rolling-item {
    padding: 0.08rem 0;
    letter-spacing: 0.4em;
    text-align: center;
  }

  @keyframes rollNames {
    0%,
    20% {
      transform: translateY(0);
    }
    33%,
    53% {
      transform: translateY(-100%);
    }
    66%,
    86% {
      transform: translateY(-200%);
    }
    100% {
      transform: translateY(0);
    }
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }
</style>
